#include<stdio.h>

int main() {
    typedef int marks;
    marks maxMarks = 100, passMarks = 33;

    printf("the maxium marks is :%d\n", maxMarks);
    printf("the pass marks are:%d\n", passMarks);

    return 0;
}